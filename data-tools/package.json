{
  "name": "data-tools",
  "version": "1.0.0",
  "description": "Extract hand keypoints from YouTube videos using MediaPipe Hands and TensorFlow.js. Manage and preprocess the dataset for training hand gesture recognition models.",
  "type": "module",
  "scripts": {
    "extract": "bun run src/extractYT.ts",
    "build": "bun run src/buildDataset.ts",
    "merge": "bun run src/merge.ts",
    "pipeline": "bun run extract && bun run build"
  },
  "dependencies": {
    "@pjm/shared": "workspace:*",
    "@tensorflow-models/hand-pose-detection": "^2.0.1",
    "@tensorflow/tfjs": "^4.22.0",
    "@tensorflow/tfjs-backend-wasm": "^4.22.0",
    "@tensorflow/tfjs-core": "^4.22.0",
    "fluent-ffmpeg": "^2.1.3",
    "sharp": "^0.34.5",
    "youtube-dl-exec": "^3.1.6"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.28"
  }
}
